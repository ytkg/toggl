# frozen_string_literal: true

RSpec.describe Toggl do
  it "has a version number" do
    expect(Toggl::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
